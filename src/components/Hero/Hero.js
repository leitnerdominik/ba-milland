import React from 'react';

import classes from './Hero.module.css';

const hero = () => (
  <header className={classes.HeroContainer}>
    <section>
      hello
    </section>
  </header>
);

export default hero;